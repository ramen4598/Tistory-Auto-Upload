# PyQt view definitions (skeleton)

from PyQt5.QtWidgets import QWidget

class MainView(QWidget):
    def __init__(self, parent=None):
        super().__init__(parent)
        # TODO: layout 및 위젯 초기화
